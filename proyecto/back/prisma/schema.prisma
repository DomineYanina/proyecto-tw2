generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model videojuego {
  id     Int    @id @default(autoincrement())
  nombre String
}
